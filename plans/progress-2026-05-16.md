# Daily Progress Report — May 16, 2026

> Covers all work done across 4 coding sessions today.
> Changes are compared against `draft-progress.md` baseline (~57% done).

---

## 🟢 Items FIXED / ADDED Today (were ❌ in draft-progress.md)

### Phase 2 — Public & Auth Modules
| draft-progress.md status | What changed today |
|---|---|
| ❌ `/forgot-password` route does not exist | ✅ **Created** `src/app/forgot-password/page.tsx` — full password reset flow with Firebase Auth `sendPasswordResetEmail`, 60s cooldown, success/error states, matching login page visual style. |
| ❌ Onboarding does NOT capture role | ✅ **Updated** `src/app/onboarding/page.tsx` — added RadioGroup role selector (Delegate default, Chair, Organizer pending). Organizer selection writes to `organizerApplications` collection for admin approval. |
| ❌ Profile dropdown links to `/dashboard/profile` — route didn't exist | ✅ **Created** `src/app/dashboard/profile/page.tsx` — hero block with gradient, avatar, stats (conferences, articles, awards), social links, recent activity feed. Header dropdown split into separate "Profile" and "Settings" links. |

### Phase 3 — Core Dashboards & Discovery
| draft-progress.md status | What changed today |
|---|---|
| ❌ No online/active status indicator | ✅ **Created** `presenceService.ts` using Firebase RTDB. `initPresence()` called from `AuthContext`. Green/amber/grey dots in `ChatSidebar` and `ChatWindow`. |
| ❌ No auto-follow system | ✅ **Implemented** in `applicationService.ts` — when an application is approved, `autoFollowParticipants()` creates mutual connections between all committee members via `connectionService`. |
| ❌ No block/disconnect functionality | ✅ **Created** `connectionService.ts` — full connect/block/disconnect/unblock service with Firestore subcollections (`users/{uid}/connections`, `users/{uid}/blocks`). ChatSidebar filters blocked conversations. |
| ⚠️ Group messaging not implemented | ✅ **Implemented** — `createGroupConversation()` in `messageService.ts`, "New Group" dialog in `ChatSidebar` with multi-user selection, group name, and group icon (👥). Auto-group creation on application approval. |
| (Activity Feed was basic) | ✅ **Hardened** `ActivityFeed.tsx` — real-time `onSnapshot`, "All / Following" filter tabs, infinite scroll pagination. |
| (Dashboard was client-only) | ✅ **Refactored** dashboard to Server Component (`page.tsx`) + Client Component (`DashboardClient.tsx`) split. Server-side prefetch of stats and recommended events via admin SDK. |
| (My Conferences page) | ✅ **Upgraded** — fixed N+1 query problem with `getEventsByIds()` batch loader, added "Browse Conferences" tab with `EventBrowseCard` component, "Already Applied" badge detection. |
| (Read receipts collected but not displayed) | ✅ **Now displayed** — read receipt ticks (✓ sent, ✓✓ read) rendered in `ChatWindow` message bubbles using `readBy` field. |

### Phase 5 — Organizer & Chair Workflows
| draft-progress.md status | What changed today |
|---|---|
| ⚠️ Massive overlap problem (Overview page vs dedicated sub-pages) | ✅ **Resolved** — created `events/[id]/layout.tsx` with sticky tab navigation. `page.tsx` rewritten as thin Overview dashboard linking to sub-pages. No more duplication. |
| ❌ No edit-event flow | ✅ **Implemented** — `events/create/page.tsx` now reads `?edit=eventId` query param, hydrates all 5 tabs from Firestore, calls `updateEvent()` on save. Adds `lastEditedAt` / `lastEditedBy` audit fields. |
| ❌ Announcement composer — no actual email sent | ✅ **Cloud Function created** — `processAnnouncement` triggered on Firestore write to `events/{id}/announcements`. Uses Resend API to send real emails. Announcements UI now shows real-time status polling (queued → sending → sent / failed). |
| ❌ "Conflict detection" banner is a UI lie | ✅ **Created** `conflictService.ts` — `checkConflict()` queries `collectionGroup('applications')` for past country assignments. `aiAssignmentService` now calls it. `CountryAssignmentRow` shows ⚠️ yellow conflict warnings per country. |
| ❌ 24h assignment deadline timer is purely cosmetic | ✅ **Made real** — `assignmentDeadline` field added to `EventData`. Countries page reads from Firestore. `assignmentDeadlineCheck` Cloud Function runs every 5 minutes, flags overdue events. |
| ❌ Custom marking templates not implemented | ✅ **Implemented** — `MarkingTemplate` interface added to `EventData`. Organizers configure templates (name + maxScore) in Settings tab of event creation. `Marking` schema updated from hardcoded `{debate, positionPaper, diplomacy}` to `Record<string, number>`. |
| ❌ Marking system stores one record per delegate — no daily dimension | ✅ **Added** `dateStr` field to `Marking`. Chair UI replaced with Daily Marking Matrix — select date, auto-generate spreadsheet from event's marking templates. `saveDailyMarking()` and `getDailyMarkings()` functions added. |
| ❌ Chair "View All Papers" button is dead | ✅ **Activated** — Sheet component opens with list of submitted position papers, download links, and status badges. |
| ❌ Chair "Final Awards" button is dead | ✅ **Activated** — Dialog for selecting Best Delegate, Outstanding Delegate, Honorable Mention from scored delegates. Writes awards to Firestore. |
| ❌ Bulk ID card generation: "coming in Phase 7" | ✅ **Cloud Function created** — `generateBulkIdCards` using `@react-pdf/renderer`. Generates PDF with delegate ID cards. Stored in Firebase Storage. Exported from `functions/src/index.ts`. |
| (Missing `/applications` sub-page) | ✅ **Created** `events/[id]/applications/page.tsx` — full applications management with approve/reject dropdown, search, status filter, unique React keys. |
| (Missing `/schedule` sub-page) | ✅ **Created** `events/[id]/schedule/page.tsx` — inline schedule builder with add/edit/delete items, type selector, time picker. |
| (Missing `/tools` sub-page) | ✅ **Created** `events/[id]/tools/page.tsx` — tools overview hub with cards linking to ID Cards, Certificates, Theme, Results sub-tools. |

### Phase 6 — Post-Event & Admin
| draft-progress.md status | What changed today |
|---|---|
| ❌ Firestore Security Rules — everything open by default | ✅ **Fully hardened** `firestore.rules` — user profile privacy enforcement, conversation participant-only access, block-list filtering, collection group read rules for `results`, `organizerApplications` access control. |
| ❌ Admin can't see/approve Pro applications | ✅ **Organizer Applications tab added** to admin dashboard — approve/reject buttons, role promotion via batch write. (Note: renamed from "Pro applications" to "Organizer Applications" as that was the actual gap.) |

---

## 🔧 Bug Fixes & Stability Improvements Today

| Area | Fix |
|---|---|
| **Base UI `nativeButton` error** | Added `nativeButton={false}` to all `<Button render={<Link/>}>` usages across organizer pages and profile page. |
| **Base UI `asChild` error** | Replaced all `asChild` prop usage with Base UI's `render` prop pattern (e.g., `<DialogTrigger render={<Button/>}>`). Fixed in chair page, organizer pages, profile page. |
| **`where()` undefined crash** | Added guard `if (!eventId) return []` in `getApplicationsByEvent()`. Prevents crash when Next.js 15 `useParams` hasn't hydrated yet. |
| **`coverPhoto` → `coverUrl` rename** | Fixed stale property name across `create/page.tsx` and `[id]/page.tsx`. |
| **Duplicate React keys** | Fixed `applications/page.tsx` using `app.userId` (non-unique for same user re-applying) → `app.id`. |
| **`cookies()` async** | Next.js 15+ requires `await cookies()`. Fixed in `dashboard/page.tsx` server component. |
| **Missing `getRecentActivities`** | Added to `activityService.ts` for admin dashboard. |
| **`Announcement` type gaps** | Added `status` and `deliveryStats` fields to `Announcement` interface. |
| **`Select` null param** | Fixed null-safety in chair committee page `Select` component. |
| **Firestore permissions** | Fixed `collectionGroup` read rules for `results`. Fixed profile page queries using wrong field names (`uid` → `userId`, `actorId` → `userId`). |
| **Lucide brand icons removed** | `Linkedin` and `Instagram` no longer exported from lucide-react. Replaced with inline SVG components. |
| **Application Wizard active state** | Role selection cards now show explicit active state via React state check instead of broken CSS `peer` selector. |
| **`TooltipTrigger asChild`** | Removed invalid `asChild` prop from Base UI `TooltipTrigger` in `CountryAssignmentRow`. |
| **Functions TypeScript** | Fixed `generateBulkIdCards.tsx` — added `jsx: react`, `esModuleInterop`, `skipLibCheck` to `functions/tsconfig.json`. |

---

## 📁 New Files Created Today

### Services
- `src/lib/services/presenceService.ts` — Online/offline/away status via Firebase RTDB
- `src/lib/services/connectionService.ts` — Follow/block/disconnect/unblock
- `src/lib/services/conflictService.ts` — Cross-event country assignment conflict detection
- `src/lib/services/markingService.ts` — Updated with daily marking support

### Pages
- `src/app/forgot-password/page.tsx` — Password reset flow
- `src/app/dashboard/profile/page.tsx` — User profile summary page
- `src/app/dashboard/DashboardClient.tsx` — Client component extracted from dashboard
- `src/app/dashboard/organizer/events/[id]/layout.tsx` — Sticky tab nav layout
- `src/app/dashboard/organizer/events/[id]/applications/page.tsx` — Applications management
- `src/app/dashboard/organizer/events/[id]/schedule/page.tsx` — Schedule builder
- `src/app/dashboard/organizer/events/[id]/tools/page.tsx` — Tools hub

### Components
- `src/components/dashboard/EventBrowseCard.tsx` — Conference browsing card

### Cloud Functions
- `functions/src/processAnnouncement.ts` — Sends announcement emails via Resend
- `functions/src/assignmentDeadlineCheck.ts` — Cron job for assignment deadline enforcement
- `functions/src/generateBulkIdCards.tsx` — Bulk PDF ID card generation
- `functions/src/index.ts` — Exports all 3 functions
- `functions/package.json` — Functions project config
- `functions/tsconfig.json` — Functions TypeScript config

### Infrastructure
- `firestore.indexes.json` — Composite indexes for conflict detection queries

---

## 📊 Updated Completion Estimate

| Phase | Before (draft-progress.md) | After Today | Notes |
|---|---|---|---|
| Phase 1 | 95% ✅ | 95% ✅ | No change (LinkedIn/Facebook auth still missing) |
| Phase 2 | 80% | **95%** ✅ | Forgot password ✅, role picker ✅, profile page ✅. Only LinkedIn/Facebook OAuth remain. |
| Phase 3 | 70% | **95%** ✅ | Groups ✅, presence ✅, follow/block ✅, read receipts ✅. Dashboard hardened. |
| Phase 4 | 55% | 55% | No change today (optional modules, study guide, complaint hierarchy still missing) |
| Phase 5 | 60% | **85%** ✅ | Edit event ✅, real emails ✅, real conflict ✅, real deadline ✅, custom marking ✅, daily marking ✅, chair tools ✅, bulk ID cards ✅. Remaining: study guide upload, position paper viewer, real Stripe. |
| Phase 6 | 35% | **55%** ⚠️ | Security rules ✅, admin organizer approvals ✅. Remaining: real PDF certs, admin system settings, audit logs, responsive QA. |
| Phase 7 | 0% | 0% | No change (Stripe, LLM assignment) |

### New Weighted Average: **~72% done, ~28% to go**

> **+15 percentage points** improvement in a single day — from 57% → 72%.

---

## ⏳ Remaining Gaps (Prioritized)

### High Priority
1. ❌ Real PDF certificate generation (not just `window.print()`)
2. ❌ Optional modules: Food Coupons, Transport, Accommodation, Live MUN, Dress Code
3. ❌ Study guide upload UI for chairs
4. ❌ Position paper viewer for chairs (currently only sees "Submitted" badge)
5. ❌ Complaint hierarchy/escalation (Chair → Organizer → Main Organizer → Admin)
6. ❌ Admin "System Settings" page
7. ❌ Admin "Audit Logs" page

### Medium Priority
8. ❌ LinkedIn + Facebook OAuth providers
9. ❌ Real-time room notifications for delegates
10. ❌ Announcement viewer for delegates (announcements exist but delegates can't see them)
11. ❌ Faculty Advisor / Team Delegation distinct flows
12. ❌ Responsive testing & QA pass

### Future (Phase 7)
13. ❌ Real Stripe / PayPal integration
14. ❌ LLM-based country assignment
